# Pliki referencyjne designu (huashu)

Pliki w tym katalogu to referencje huashu, celowo **bez frontmattera**, ładowane ścieżką z `design/pipeline.md` (`references/<file>.md`).

**NIE DODAWAJ TU FRONTMATTERA.**
Dodanie frontmattera uczyniłoby te pliki jedynymi widocznymi skillami kategorii `design` w całym systemie w rejestrze `SkillRegistry`, a największe z nich (`design-styles.md` 54 839 zn., `slide-decks.md` 41 770 zn.) przekraczają budżet półki (`maxActiveChars: 36000`) i byłyby odrzucane przy ładowaniu.
